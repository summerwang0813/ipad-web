interface:
  display_name: "Figma Page Sync"
  short_description: "Write any website back to Figma accurately"
  default_prompt: "Use $figma-page-sync to write the current website back to Figma using the component library, variables, images, and exact webpage layout."

policy:
  allow_implicit_invocation: true
